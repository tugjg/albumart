import React from 'react';


const validateImage = (image) => {
    // return bool, does image meet upload standards (type, size, dimensions, etc)
}

const Uploader = () => {
    return (
        <div>
            <button>Upload</button>
        </div>
    );
}
